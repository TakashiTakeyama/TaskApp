<h3><%= t('view.user_show') %></h3>
<p>
  <strong><%= t('view.user_name') %></strong>
  <%= @user.name %>
</p>

<p>
  <strong><%= t('view.user_email') %></strong>
  <%= @user.email %>
</p>

<h3><%= "#{@user.name}""#{t('view.index_task_all')}" %></h3>

<table class="table">
  <thead>
  <tr>
    <th>name</th>
    <th>details</th>
    <th>created_at</th>
  </tr>
  </thead>

  <tbody>
  <% @user.blogs.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.details %></td>
      <td><%= l user.created_at, format: :short %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= link_to "edit", edit_admin_user_path(@user) %><br>
<!--<%#= link_to "#{t('view.user_task')}", new_blog_path %><br>-->
<!--<%#= link_to "#{t('view.back_index')}", users_path %><br>-->
<%#= link_to "#{t('view.admin_all_user')}", admin_users_path %>

