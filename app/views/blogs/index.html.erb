<p id="notice"><%= notice %></p>

<h1>Tasks</h1>

<table>
  <thead>
  <tr>
    <th>duty</th>
    <th>Details</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @blogs.each do |blog| %>
    <tr>
      <td><%= blog.name %></td>
      <td><%= blog.details %></td>
      <td><%= link_to '詳細', blog %></td>
      <td><%= link_to '編集', edit_blog_path(blog) %></td>
      <td><%= link_to '削除', blog, method: :delete, data: { confirm: '本当にいいですか?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'タスクを追加ぁぁぁぁぁ!!!', new_blog_path %>
